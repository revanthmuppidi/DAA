Summary:
In the practical we successfully implemented the coin exchange problem
using dynamic programming. This program calculates the least no of
coins required for paying, this programs stores all the previous values dp
array rather than calculating it again and again. Time complexity for the
calculating the amount is O(n*a) n is no of coins, a is amount, space
complexity is O(a) due to its dparry. The approach is best for handling
large data rather than the greedy approach while greedy give the output
faster than the dynamic method but not efficient.therefore we consider the
dynamic method for solving the large datasets


Conclusion:
The Coin Change program successfully finds the minimum number of coins needed to make a given amount. It uses the Dynamic Programming method to solve the problem efficiently. The program also checks whether the amount can be formed using the given coins. The execution time is measured, and the time and space complexity are also shown. Overall, this program helps us understand the practical use of Dynamic Programming in solving optimization problems.